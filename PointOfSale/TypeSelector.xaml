<UserControl x:Class="PointOfSale.TypeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Button Click="SelectEntree" Grid.Row="0" FontSize="20" FontFamily="Arial">
            Entree
        </Button>
        <Button Click="SelectSide" Grid.Row="1" FontSize="20" FontFamily="Arial">
            Side
        </Button>
        <Button Click="SelectDrink" Grid.Row="2" FontSize="20" FontFamily="Arial">
            Drink
        </Button>
    </Grid>
</UserControl>
